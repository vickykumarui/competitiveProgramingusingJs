function power(m,n){
    var factVal = 1;

    function helper(m,n){
        if(n === 0){
            return 1
        }

        factVal = factVal*m;
        helper(m,n-1);
    }

    helper(m,n)

    return factVal;
}